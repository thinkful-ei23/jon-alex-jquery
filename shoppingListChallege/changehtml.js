'use strict';

const createItem = function(item) {
  return `
    <li>
      ${item.name}
      <button class="delete">
        <span>Delete</span>
      </button>
    </li>
  `;
};


$(function() {

  $('#add-item').on('click', () => )

})